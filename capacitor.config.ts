import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.moneyschemes.preppro',
  appName: 'RoleCall',
  webDir: 'www',
  ios: {
    contentInset: 'automatic',
    backgroundColor: '#0f0f1a'
  }
};

export default config;
